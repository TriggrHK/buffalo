@{
}

@model IEnumerable<string>
<div class="accordion-item mt-2">
    <h2 class="accordion-header" id="heading3">
        <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse3" aria-expanded="true" aria-controls="collapse3">
            <h5>Structure Function</h5>
            <i class="bi bi-chevron-down mx-auto"></i>
        </button>
    </h2>
    <div id="collapse3" class="accordion-collapse collapse" aria-labelledby="heading3" data-bs-parent="#accordionExample">
        <div class="accordion-body d-grid gap-2">
            @foreach (string t in Model)
            {
                <a asp-controller="Home" asp-action="Burials" asp-route-SFunctionFilter="@t" asp-route-pageNum="1"
                   class="btn @(ViewBag.SelectedSFunction == t ? "btn-primary" : "btn-outline-secondary")">@t</a>
            }
        </div>
    </div>
</div>



@*<div class="gap-2 mt-2">
    <h3>Structure Function</h3>
    @foreach (string t in Model)
    {
        <a asp-controller="Home" asp-action="Burials" asp-route-SFunctionFilter="@t" asp-route-pageNum="1"
           class="btn @(ViewBag.SelectedSFunction == t ? "btn-primary" : "btn-outline-secondary")">@t</a>
    }
</div>*@
